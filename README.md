# 🧩 Backend — FastAPI Application

## 📖 Описание проекта
Этот репозиторий содержит **backend-приложение**, разработанное на **FastAPI**.  
Приложение реализует REST API для работы с базой данных, использует ORM **SQLAlchemy**, схемы **Pydantic** и систему миграций **Alembic**.

Проект предназначен для обработки запросов к серверу, управления данными и предоставления интерфейсов для внешних сервисов и клиентов (веб или мобильных приложений).

---

## 🧱 Архитектура

Проект построен по модульной архитектуре:

domain/ — бизнес-логика, сервисные классы, репозитории;

models/ — ORM-модели SQLAlchemy и схемы Pydantic;

database.py — инициализация подключения к БД;

main.py — точка входа, настройка FastAPI-приложения, подключение маршрутов.

---

## ⚙️ Технологии

| Компонент | Описание |
|------------|-----------|
| **Python 3.10+** | Язык программирования |
| **FastAPI** | Асинхронный веб-фреймворк |
| **SQLAlchemy** | ORM для работы с базой данных |
| **Alembic** | Управление миграциями |
| **Pydantic** | Валидация и сериализация данных |
| **Uvicorn** | ASGI-сервер для запуска приложения |

---

### 1️⃣ Клонирование репозитория
```bash
git clone https://github.com/bailiev7/backend.git
cd backend
```

###2️⃣ Создание виртуального окружения
```
python -m venv venv
source venv/bin/activate       # Linux / macOS
venv\Scripts\activate          # Windows
```
3️⃣ Установка зависимостей
```pip install -r requirements.txt```

4️⃣ Настройка переменных окружения

Создай файл .env в корне проекта и добавь туда строку:

```DATABASE_URL=postgresql://bailiev:bailiev@localhost:5432/backend```

5️⃣ Применение миграций базы данных
```alembic upgrade head```

6️⃣ Запуск приложения
```uvicorn main:app --reload```


## ☁️ Развёртывание

Для продакшн-среды можно использовать Gunicorn + Uvicorn:

```gunicorn -k uvicorn.workers.UvicornWorker main:app --bind 0.0.0.0:8000```

---
